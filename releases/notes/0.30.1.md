<!-- zh-Hans -->
## 修复
- 修复 Qoder / Qoder CN 统计开关污染宿主识别的问题，启用或关闭统计均不会将无关进程误识别为 IDE
- 综合 Codex Desktop 来源与真实终端、TTY、tmux、父进程证据判断 App / CLI，保留实际运行于 IDE 终端的 Codex CLI
- 自动修正已确认受污染的会话缓存和当前会话，清除错误 Qoder 宿主及工作区跳转地址，恢复 Codex 线程跳转
- 修复旧缓存将真实终端 CLI 标为 Codex App 后无法恢复的问题
- 统一识别标题生成、标题更新和建议生成辅助任务，覆盖 hook、快照、日志恢复和会话列表
- 保留正常 JSON 回复、根目录用户任务及用户点击建议后创建的任务，避免按回复结构误过滤

## 说明
- 版本号为 `0.30.1`，构建号为 `81`
- 保持旧会话缓存兼容，升级时根据已保存的来源证据自动修正；证据不足的旧记录在收到新的来源信息后修正
- 手动安装优先使用 DMG；ZIP 保留用于更新与分发

<!-- en -->
## Fixes
- Prevents Qoder and Qoder CN statistics flags from identifying unrelated processes as IDE hosts, whether enabled or disabled
- Combines Codex Desktop source metadata with actual terminal, TTY, tmux, and parent-process evidence to preserve genuine IDE-hosted Codex CLI sessions
- Repairs confirmed contaminated cached and live session routing, removes false Qoder hosts and workspace links, and restores Codex thread navigation
- Restores genuine CLI routing for older cached sessions incorrectly labeled as Codex App
- Identifies title generation, title updates, and suggestion helpers consistently across hooks, snapshots, rollout recovery, and restored session lists
- Preserves ordinary JSON replies, root-directory user tasks, and tasks created by accepting suggestions instead of filtering by output structure

## Notes
- App version `0.30.1`, build `81`
- Existing session caches remain readable. Stored source evidence is repaired during loading; ambiguous older records are repaired when new source information arrives
- Use the DMG for manual installation; the ZIP remains available for update and distribution workflows
