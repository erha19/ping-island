<!-- zh-Hans -->
## 兼容性
- 兼容 QoderWork `0.6.7+` 版本的全新 Hooks 链路。
- QoderWork 的 `AskUserQuestion` 现在可在 Island 内直接提交答案，同时 `expectsResponse=false` 的工具通知不会弹出审批。

## 说明
- Sparkle build 号提升到 `62`，版本号提升到 `0.24.2`。
- 本次发布基于 `v0.24.1` 之后的 latest main，重点覆盖 QoderWork `0.6.7+` Hooks 兼容性。

<!-- en -->
## Compatibility
- Adds compatibility with the new Hooks flow in QoderWork `0.6.7+`.
- QoderWork `AskUserQuestion` prompts can now be answered directly in Island, while `expectsResponse=false` tool notifications stay non-blocking.

## Notes
- The Sparkle build number is now `62`, and the app version is now `0.24.2`.
- This release is based on latest `main` after `v0.24.1` and focuses on QoderWork `0.6.7+` Hooks compatibility.
